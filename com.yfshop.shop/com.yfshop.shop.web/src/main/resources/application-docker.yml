---   
server:
  port: 9091
dubbo:
  registry:
    address: zookeeper://${ZOOKEEPER_HOST:192.168.11.197}:${ZOOKEEPER_PORT:2181}
    dynamic: true
  metadataReport:
    address: zookeeper://${ZOOKEEPER_HOST:192.168.11.197}:${ZOOKEEPER_PORT:2181}
  configcenter:
    address: zookeeper://${ZOOKEEPER_HOST:192.168.11.197}:${ZOOKEEPER_PORT:2181}
  application:
    name: yfshop-shop-web
  consumer:
    timeout: 30000
    retries: 0
    check: false
  scan:
    base-packages: com.yfshop.shop.com.yfshop.open.controller
    
    
spring:
    redis:
        database: 0
        host: ${REDIS_HOST:192.168.11.197}:${REDIS_PORT:6379}
        password: ''
        jedis:
          pool:
            max-active: 200
            max-idle: 10
            max-wait: -1
            min-idle: 0
        port: 6379
        timeout: 1000

