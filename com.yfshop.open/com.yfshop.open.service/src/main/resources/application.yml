---
spring:
  application:
    name: yfshop-shop-service
  main:
    web-application-type: none
  profiles:
    active: @profiles.active@

mybatis-plus:
  mapper-locations:
    - classpath:dao/*.xml
    - classpath*:com/**/mapper/*.xml
